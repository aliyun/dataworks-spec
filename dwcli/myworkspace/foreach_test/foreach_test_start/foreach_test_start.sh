#!/bin/bash
# Traverse start controller for foreach_test
# This script marks the beginning of each iteration

echo "[foreach_test_start] Starting iteration: ${loop_index}"
echo "[foreach_test_start] Current item: ${item}"